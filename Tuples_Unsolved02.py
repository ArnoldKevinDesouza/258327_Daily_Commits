# Write a Python program to convert a list of tuples into a dictionary
 
lis = [("A", 1), ("B", 2), ("C", 3), ("D", 4)] 
print("The list is -> ", lis)
di = dict(lis) 
print ("The list converted into dictionary -> ",di)